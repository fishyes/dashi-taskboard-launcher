# v1.1.6

修正新版 Codex 側欄將導航按鈕包進 `div.contents` 後，任務面板入口未建立的問題。

## Fixes

- 以「外掛程式」所在的導航群組驗證穩定錨點，不再要求導航按鈕必須是直接 sibling。
- 保留既有 exact label、側欄範圍與最少三個導航按鈕限制，避免命中非導航區域。
- 注入腳本版本更新至 `0.6.18`。

## 驗證

- `node --test test/inject.test.mjs test/injector.test.mjs test/codex-cdp-pipe.test.mjs test/codex-targets-filter.test.mjs`
- `npm run build:web`
- `pnpm test`
- `pnpm build`
- `cargo test --manifest-path src-tauri/Cargo.toml`
- `pnpm run check:release -- --tag v1.1.6`
- `pnpm tauri build --bundles nsis`
- 對目前 Codex renderer 執行唯讀 DOM 驗收，確認 `codex-taskboard-entry` 存在。
